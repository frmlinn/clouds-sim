const GLUtils={init(canvas){const gl=canvas.getContext("webgl2",{antialias:false,depth:false,alpha:false,preserveDrawingBuffer:false});if(!gl){alert("Your browser does not support WebGL 2. It is required for 3D textures.");throw new Error("WebGL 2 not supported.")}const extFloat=gl.getExtension("EXT_color_buffer_float");const extHalfFloat=gl.getExtension("EXT_color_buffer_half_float");if(!extFloat&&!extHalfFloat){console.warn("[GLUtils] Warning: Floating-point texture rendering might not be fully supported on this device.")}return gl},compileShader(gl,type,source){const shader=gl.createShader(type);gl.shaderSource(shader,source);gl.compileShader(shader);if(!gl.getShaderParameter(shader,gl.COMPILE_STATUS)){const shaderType=type===gl.VERTEX_SHADER?"Vertex":"Fragment";console.error(`[GLUtils] Error compiling ${shaderType} Shader:\n`,gl.getShaderInfoLog(shader));gl.deleteShader(shader);return null}return shader},createProgram(gl,vsSource,fsSource){const vertexShader=this.compileShader(gl,gl.VERTEX_SHADER,vsSource);const fragmentShader=this.compileShader(gl,gl.FRAGMENT_SHADER,fsSource);if(!vertexShader||!fragmentShader)return null;const program=gl.createProgram();gl.attachShader(program,vertexShader);gl.attachShader(program,fragmentShader);gl.linkProgram(program);if(!gl.getProgramParameter(program,gl.LINK_STATUS)){console.error("[GLUtils] Error linking WebGL program:",gl.getProgramInfoLog(program));gl.deleteProgram(program);return null}gl.detachShader(program,vertexShader);gl.detachShader(program,fragmentShader);gl.deleteShader(vertexShader);gl.deleteShader(fragmentShader);return program},create3DTexture(gl,size,data){const texture=gl.createTexture();gl.bindTexture(gl.TEXTURE_3D,texture);gl.texParameteri(gl.TEXTURE_3D,gl.TEXTURE_MIN_FILTER,gl.LINEAR);gl.texParameteri(gl.TEXTURE_3D,gl.TEXTURE_MAG_FILTER,gl.LINEAR);gl.texParameteri(gl.TEXTURE_3D,gl.TEXTURE_WRAP_S,gl.REPEAT);gl.texParameteri(gl.TEXTURE_3D,gl.TEXTURE_WRAP_T,gl.REPEAT);gl.texParameteri(gl.TEXTURE_3D,gl.TEXTURE_WRAP_R,gl.REPEAT);gl.pixelStorei(gl.UNPACK_ALIGNMENT,1);gl.texImage3D(gl.TEXTURE_3D,0,gl.R8,size,size,size,0,gl.RED,gl.UNSIGNED_BYTE,data);gl.pixelStorei(gl.UNPACK_ALIGNMENT,4);return texture},createFBO(gl,width,height,internalFormat=null,format=null,type=null){const tex=gl.createTexture();gl.bindTexture(gl.TEXTURE_2D,tex);const iFormat=internalFormat||gl.RGBA16F;const fmt=format||gl.RGBA;const t=type||gl.HALF_FLOAT;gl.texImage2D(gl.TEXTURE_2D,0,iFormat,width,height,0,fmt,t,null);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_MIN_FILTER,gl.LINEAR);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_MAG_FILTER,gl.LINEAR);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_WRAP_S,gl.CLAMP_TO_EDGE);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_WRAP_T,gl.CLAMP_TO_EDGE);const fbo=gl.createFramebuffer();gl.bindFramebuffer(gl.FRAMEBUFFER,fbo);gl.framebufferTexture2D(gl.FRAMEBUFFER,gl.COLOR_ATTACHMENT0,gl.TEXTURE_2D,tex,0);const status=gl.checkFramebufferStatus(gl.FRAMEBUFFER);if(status!==gl.FRAMEBUFFER_COMPLETE){console.error(`[GLUtils] Error creating FBO (Status: ${status}). Ensure float/half-float extensions are enabled.`)}gl.bindFramebuffer(gl.FRAMEBUFFER,null);return{fbo:fbo,tex:tex,width:width,height:height,internalFormat:iFormat,format:fmt,type:t}},resizeFBO(gl,fboObj,width,height){if(!fboObj||fboObj.width===width&&fboObj.height===height)return;gl.bindTexture(gl.TEXTURE_2D,fboObj.tex);gl.texImage2D(gl.TEXTURE_2D,0,fboObj.internalFormat,width,height,0,fboObj.format,fboObj.type,null);fboObj.width=width;fboObj.height=height},loadTexture2D(gl,url){const texture=gl.createTexture();gl.bindTexture(gl.TEXTURE_2D,texture);gl.texImage2D(gl.TEXTURE_2D,0,gl.RGBA,1,1,0,gl.RGBA,gl.UNSIGNED_BYTE,new Uint8Array([128,128,128,255]));const image=new Image;image.crossOrigin="anonymous";image.src=url;image.onload=()=>{gl.bindTexture(gl.TEXTURE_2D,texture);gl.texImage2D(gl.TEXTURE_2D,0,gl.RGBA,gl.RGBA,gl.UNSIGNED_BYTE,image);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_WRAP_S,gl.REPEAT);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_WRAP_T,gl.REPEAT);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_MIN_FILTER,gl.NEAREST);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_MAG_FILTER,gl.NEAREST)};image.onerror=()=>{console.error(`[GLUtils] Failed to load texture from URL: ${url}`)};return texture}};